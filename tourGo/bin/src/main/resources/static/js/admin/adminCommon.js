/**
const searchBtn = document.getElementById('searchBtn');

if(searchBtn) {
	searchBtn.addEventListener("click", function() {
		const selectBox = document.getElementById('selectBox');
		
		if(!selectBox.value || selectBox.value == "") {
			throw new Error("조건을 선택해주세요");
		}
	});
}
 */
	
